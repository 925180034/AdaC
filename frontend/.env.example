# Leave empty for Vite same-origin proxy demo mode.
# Set to http://localhost:8080 only when the browser should call FastAPI directly.
VITE_API_BASE_URL=
VITE_API_KEY=dev-local-token
VITE_DEFAULT_TENANT_ID=default
